// it is shortcut method to use CONDITIONS IN JS 

let age = 20; // if
let message = age >= 14 ? "you are an adult" : "you are minor";
                            //if age >>>          // else >>>>>>

// it  is same to


if (age === 20) {
    message = "you are an adult";
    console.log(message);
}
else {
    message = " you are minor";
    console.log(message);
}